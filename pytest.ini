[pytest]
addopts = -ra -m "not network"
markers =
    network: tests requiring live NIST Chemistry WebBook access
    rdkit: tests requiring RDKit
    slow_network: slow live NIST Chemistry WebBook tests
